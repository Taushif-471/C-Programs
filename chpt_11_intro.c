#include <stdio.h>
#include <stdlib.h>
 int main(){
//  int *ptr;
// int*bet;
// float*ted;
// double*wet;
//      //size of operators in in c in bytes,
     printf("the size of int in my pc in bytes is:%d\n", sizeof(int));
     printf("the size of float in my pc in bytes is:%d\n", sizeof(float));
     printf("the size of character in my pc in bytes is:%d\n", sizeof(char));
//      ptr=(char*)malloc(10* sizeof(int));
//      bet=(int*)malloc(10*sizeof(float));
//      ted=(float*)malloc(10*sizeof(char));
//      wet=(double*)malloc(10*sizeof(float));
//      printf("char in bytes=%d\n int in bytes=%d\n float in bytes=%d\n double in byte=%d\n",(*ptr),(*bet),
//      (*ted),(*wet));
    return 0;}