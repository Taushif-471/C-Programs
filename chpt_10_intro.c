#include <stdio.h>
int main(){
    FILE*ptr;
   // ptr=fopen("file name.txt","w";);  //"w"for writing something in the file of the  program
    ptr=fopen("file name.txt","r");  //"r" for reading a program after opening a file with this syntax
        return 0;}                   //of this program.