#include <stdio.h>
int main(){ char a;
printf("enter the char a=");
scanf("%c",&a);
printf("the ASCII value of a is= %d\n",a);
if((a)>=97 && (a)<=122){
    printf(" so the character is lower case");}
    else if((a)>=65 && (a)<=90) {
        printf("the character is upper case ");}}



