#include <stdio.h>
int main(){
    char s[50];
    printf("enter your name:");
    gets(s);
    puts(s);  //puts()function is used to print multi-words of the string in the console
               //when printf() is not used 
     
}